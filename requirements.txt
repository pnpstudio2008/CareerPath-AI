# AI Career Companion — Python Dependencies
# Install via: pip install -r requirements.txt
# Hostinger hPanel: paste each line in the pip install field

flask>=3.0.0
gunicorn>=21.2.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pypdf>=5.0.0
requests>=2.31.0
